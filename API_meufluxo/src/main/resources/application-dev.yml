spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/meufluxo_db_dev
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format-sql: true

  kafka:
    bootstrap-servers: localhost:9092

  flyway:
    enabled: true
